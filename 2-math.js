const addNumber = (x, y) => console.log(x + y);

const PI = Math.PI;

const square = (num) => console.log(num * num);

// module.exports.addNumber = addNumber;
// module.exports.PI = PI;
// module.exports.square = square;

module.exports = { addNumber, PI, square };

// console.log(module);
